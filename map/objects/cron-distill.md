---
type: object
cluster: infrastructure
universe: live
status: stub
entity: /opt/data/cron/jobs.json (job_id 7a1a02bde863)
---

# cron-distill

Job `7a1a02bde863` (`agent-research-distill`, Sun 18:00 UTC). Forces the distillation prompt per PROTOCOL.md Phase 2 and writes `distillation/<date>.md`.

> This card is intentionally a stub. Infrastructure nouns (cron jobs, notebooks, protocols) get filled when they're directly cited by a capability card (slice 2 follow-up) or when they need a change-impact entry (slice 4).
